@model global::Tribo.Models.Contato

@{
    ViewData["Title"] = "Administracao";
}

<div class="bgVidro rounded text-center p-3">
    <h1>Administração - Contatos</h1>
</div>

<div class="container-fluid" id="ModalAqui">
</div>


<div class="container-fluid m-0 bgVidro shadow-lg rounded py-3">
    <table class="table text-white">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Nome)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Email)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Mensagem)
                </th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in ViewBag.contatos)
            {
                <tr>
                    <td>
                        @item.Nome
                    </td>
                    <td>
                        @item.Email
                    </td>
                    <td>
                        @item.Mensagem
                    </td>
                    <td>
                        <button type="button" class="btn btn-sm btn-secondary   btn-outline-warning" data-bs-toggle="modalAjax" data-bs-target="#editarModalAdmCnt"
                            data-url="@Url.Action($"EditContato/{item.IdContato}")">
                            <img src="~/img/edit.png" />
                        </button>

                        <button type="button" class="btn btn-sm btn-secondary   btn-outline-warning mt-1" data-bs-toggle="modalAjax" data-bs-target="#detalhesModalAdmCnt"
                            data-url="@Url.Action($"DetailsContato/{item.IdContato}")">
                            <img src="~/img/ver-detalhes.png" />
                        </button>

                        <button type="button" class="btn btn-sm btn-secondary   btn-outline-warning  mt-1" data-bs-toggle="modalAjax" data-bs-target="#deleteModalAdmCnt"
                            data-url="@Url.Action($"DeleteContato/{item.IdContato}")">
                            <img src="~/img/delete.png" />
                        </button>
                    </td> 
                </tr>
            }
        </tbody>
    </table>
</div>


@section Scripts {
    @{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
}
}

